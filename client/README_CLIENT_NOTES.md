Client notes:
- Tailwind is configured via tailwind.config.cjs and postcss.config.cjs
- Vite config does NOT import @tailwindcss/vite; use standard setup
- To enable tailwind: run `npm install -D tailwindcss postcss autoprefixer` and `npx tailwindcss init -p`
